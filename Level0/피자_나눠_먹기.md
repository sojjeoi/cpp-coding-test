# 피자 나눠 먹기 (2) (Level 0)

## 문제
n명이 모두 같은 양의 피자를 먹으려 할 때 시켜야 하는 최소 피자 판 수를 반환하라. (한 판 = 6조각)

## 접근
6과 n의 최소공배수를 구해 6으로 나누면 필요한 판 수.

## 코드
```cpp
#include <numeric>
int solution(int n) {
    return lcm(6, n) / 6;
}
```

## 사용 함수
| 함수 | 용도 |
|---|---|
| `lcm` | 두 수의 최소공배수 계산 |
